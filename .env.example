# Public variables are exposed to the browser. Use NEXT_PUBLIC_ prefix (replaces Vite’s VITE_).
# Used for invite/signup links when running on localhost (optional).
# NEXT_PUBLIC_APP_URL=https://your-production-domain.com
